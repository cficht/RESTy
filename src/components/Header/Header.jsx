import React from 'react';
import styles from './Header.css';

const Header = () => (
  <div className={styles.Header}>
    <h1>RESTy</h1>
    <p>the simple ReST Client</p>
  </div>
);

export default Header;
